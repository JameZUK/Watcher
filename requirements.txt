fastapi==0.115.6
uvicorn[standard]==0.34.0
jinja2==3.1.5
python-multipart==0.0.20
itsdangerous==2.2.0
sqlalchemy[asyncio]==2.0.36
aiosqlite==0.20.0
pydantic==2.10.4
pydantic-settings==2.7.1
apscheduler==3.11.0
httpx==0.28.1
argon2-cffi==23.1.0
cryptography==44.0.0
pywebpush==2.0.3
playwright==1.49.1
camoufox[geoip]==0.4.11
Pillow==11.1.0
pixelmatch==0.3.0
deepdiff==8.1.1
beautifulsoup4==4.12.3
# lxml is optional: it speeds up HTML parsing, but Watcher falls back to the
# stdlib html.parser if it is unavailable (e.g. on very new Python versions
# without prebuilt wheels). Uncomment to enable.
# lxml==5.3.0

# Interruptible regex (per-call timeout) to defuse ReDoS in user ignore-patterns
regex==2024.11.6
